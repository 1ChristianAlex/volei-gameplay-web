import { CommonPagePading } from '@theme/GlobalStyles';
import { styledApp } from '@theme/theme';

const PageCenterContainer = styledApp(CommonPagePading)({
  margin: '0 auto',
  display: 'flex',
});

export { PageCenterContainer };
